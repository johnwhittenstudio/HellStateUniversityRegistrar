@{
  Layout = "_Layout";
}

@model UniversityRegistrar.Models.Student
<div class="container">
<br>
<h2>Student Details</h2>
<hr />
<h3>@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</h3>
<h5>@Html.DisplayNameFor(model => model.Enrolled): @Html.DisplayFor(model => model.Enrolled)</h5>
<br>

@* @if (@Model.Departments.Count == 0)
{
  <p>This student has not delcared a major in any departments</p>
}
else
{
  <h4>This student's major is:</h4>
  <ul>
  @foreach (var join in Model.Departments)
    {
      <li>@join.Department.DepartmentName</li>
      @using (Html.BeginForm("DeleteDepartment", "Students"))
      {
        @Html.Hidden("joinId", @join.CourseDepartmentStudentId)
        <input type="submit" value="Delete" />
      }
    }
  </ul>
} *@

@if (@Model.Courses.Count == 0)
{
  <p>This student is not enrolled in any courses</p>
}
else
{
  <h4>Courses the student is enrolled in:</h4>
  <ul>
  @foreach (var join in Model.Courses)
    {
      <li>@join.Course.Name</li>
      @using (Html.BeginForm("DeleteCourse", "Students"))
      {
        @Html.Hidden("joinId", @join.CourseDepartmentStudentId)
        <input type="submit" value="Delete" />
      }
    }
  </ul>
}
<br>
<p>@Html.ActionLink("Enroll in a Course", "AddCourse", new { id = Model.StudentId })</p>
<p>@Html.ActionLink("Edit Student", "Edit", new { id = Model.StudentId })</p>
<p>@Html.ActionLink("Delete Student", "Delete", new { id = Model.StudentId })</p>
<p>@Html.ActionLink("Back to Students", "Index")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>
</div>